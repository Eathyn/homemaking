<view class="action-bar-item">
  <button bind:tap="handleChat">联系对方</button>
  <button
    wx:if="{{ service.type === serviceTypeEnum.PROVIDE }}"
    bind:tap="handleOrder"
  >
    立即预约
  </button>
</view>
